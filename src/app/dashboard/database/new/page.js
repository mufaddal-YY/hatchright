// sections
import { DatabaseCreateView } from 'src/sections/database/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new Database',
};

export default function DatabaseCreatePage() {
  return <DatabaseCreateView />;
}
